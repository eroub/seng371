import "mocha";
import chai from "chai";
import { Server } from "../../src/app"
import { Helpers } from "../../src/helperFunctions";
const request = require('supertest');

const serve = new Server();

describe('Testing Functionality of the Helper Functions:', () => {
    it('getAllUserShoes', async () => {

    }).timeout(5000);
    it('getUserShoes', async () => {
        
    }).timeout(5000);
    it('getUserInfo', async () => {
        
    }).timeout(5000);
    it('getShoeInfo', async () => {
        
    }).timeout(5000);
    it('getUserKeys', async () => {
        
    }).timeout(5000);
    it('isUser', async () => {
        
    }).timeout(5000);
    it('getAllDbShoes', async () => {
        
    }).timeout(5000);
    it('findUserShoe', async () => {
        
    }).timeout(5000);
    it('setNet', async () => {
        
    }).timeout(5000);
    it('getUsers', async () => {
        
    }).timeout(5000);
    it('getShoe', async () => {
        
    }).timeout(5000);
});